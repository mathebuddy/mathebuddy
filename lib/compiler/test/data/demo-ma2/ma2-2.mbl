Differentialgleichungen
#######################

% TODO: migrate old SELL ODE exercises

EXERCISE DGL erster Ordnung mit trennbaren Variablen
    CODE
        let a = rand(2,5)
        %TODO: let y(x) = ode( diff(y,x), - a * x^2/y )
    TEXT
        Löse die folgende DGL: $y$:
        - $"y"(x) = \pm$ #y
        *Hinweis: Gebe die Konstante $C \in \RR$ mit einem großen `C`, sowie die Wurzel von $x$ mit `sqrt(x)` an.*
