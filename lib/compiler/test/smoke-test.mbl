EXERCISE 
    @options
    %choices-4
    %timed-60
    CODE
        let r=rand(6,12)
        let phi = rand({pi/2, pi, -pi, -pi/2});
        let z= r*exp(phi*1i);
    Was ist die Normalform von $"z"$ mit Polarkoordinaten $(r,phi)$?
    $"z"=$ #z
