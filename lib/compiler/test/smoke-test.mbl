
EXERCISE
    CODE
        let r= rand(3,8);
        let a= randZ(-1,1);
        let tmp = rand({pi, pi/2});
        let phi= a*tmp;
        let z= r*cos(phi)+1i*r*sin(phi);
    Gib die komplexe Zahl $"z"=r e^{@@phi i}$ in Normalform an 

    $"z"=$ #z




%EXERCISE
%    CODE
%        let r= rand(3,8);
%        let a= randZ(-1,1);
%        let phi= a*rand({pi, pi/2})
%        let z= r*cos(phi)+1i*r*sin(phi);
%    Gib die komplexe Zahl $"z"=r e^{@@phi i}$ in Normalform an 
%
%    $"z"=$ #z
